namespace Literasi.Models.Enums;

public enum Gender
{
    L,
    P
}

public enum MaterialType
{
    PDF,
    PPT,
    LINK
}

public enum SubmissionType
{
    PDF,
    LINK
}

public enum SubmissionStatus
{
    Submitted,
    Late,
    Reviewed
}   

